dojo.provide("t.unit.plugins.workflow.userworkflows.test");

if(dojo.isBrowser){
     //Define the HTML file/module URL to import as a 'remote' test.
     doh.registerUrl(
          "t.unit.plugins.workflow.userworkflows.test", 
          dojo.moduleUrl("t", "unit/plugins/workflow/userworkflows/test.html"));
}
